import { CSSProperties } from '@material-ui/core/styles/withStyles';

export const slide: CSSProperties = {
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'start',
  fontSize: 'calc(10px + 2vmin)',
};
